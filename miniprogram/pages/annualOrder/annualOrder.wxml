<view>
    <view class="intframe">
        <view>
            <van-dropdown-menu>
                <van-dropdown-item bindchange="hospital" value="{{ value1 }}" options="{{ option1 }}" />
            </van-dropdown-menu>

            <!-- <van-dropdown-menu>
        <van-dropdown-item bindchange="time" value="{{ value2 }}" options="{{ option2 }}" />
        <import src="1test.wxml"/>
        <template is="1test" data="{{text: 'forbar'}}"/>
    </van-dropdown-menu> -->
            <view class="calendar">
                <van-cell title="选择单个日期" value="{{ date }}" bind:click="onDisplay" />
                <van-calendar show="{{ show }}" bind:close="onClose" bind:confirm="onConfirm"
                    color="linear-gradient(#0381ff, #1f4fdb)" />
            </view>
            <button bindtap="Success" class="btn_order">预约接种</button><!-- 跳转首页 -->

        </view>
    </view>
</view>